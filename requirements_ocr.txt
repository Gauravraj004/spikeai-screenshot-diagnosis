# OCR requirements - CPU-only PyTorch + EasyOCR
# Install AFTER base requirements
--index-url https://download.pytorch.org/whl/cpu
torch==2.0.1+cpu
torchvision==0.15.2+cpu
--index-url https://pypi.org/simple
easyocr>=1.7.0
