# SpikeAI - Web Scraping Validation Pipeline
# 4-Stage Integrated Diagnosis: Global CV + Regional CV + OCR + LLM

# Core Dependencies (Required)
beautifulsoup4>=4.12.0
lxml>=4.9.0
numpy>=1.24.0
opencv-python>=4.8.0
pillow>=10.0.0
scikit-image>=0.21.0
aiohttp>=3.9.0
requests>=2.31.0
openpyxl>=3.1.0
python-groq>=0.4.0  # LLM API for Stage 2
python-dotenv>=1.0.0

# OCR Dependencies (Stage 1.75 - Optional but Recommended)
# Note: Both EasyOCR and PaddleOCR require PyTorch
# On Windows, may need Visual C++ Redistributables: https://aka.ms/vs/17/release/vc_redist.x64.exe

# Option A: EasyOCR (better accuracy, slower)
# easyocr>=1.7.1
# torch>=2.1.0
# torchvision>=0.16.0

# Option B: PaddleOCR (faster, Windows-friendly)
paddleocr>=3.3.0
paddlepaddle>=3.2.0

# Note: If OCR fails to initialize (PyTorch DLL errors), pipeline will continue with CV+LLM only
# Accuracy without OCR: 100% on current test cases (8/8)
# Accuracy with OCR: Expected 100% with higher confidence on edge cases

# Development & Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Image processing enhancement
imagehash>=4.3.1
